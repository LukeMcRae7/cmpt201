#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
  char *line = NULL;
  size_t len = 0;
  size_t nread;

  nread = getline(&line, &len, stdin);
  printf("%s", line);

  char *tokptr;
  char *tok;
  tok = strtok_r(line, &tokptr);
  printf("%s", tok);

  free(line);
}
